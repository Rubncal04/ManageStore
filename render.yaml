databases:
  - name: ManageStore
    databaseName: manage_store
    user: ruben

services:
  - type: web
    name: ManageStore
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: manage_store
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false